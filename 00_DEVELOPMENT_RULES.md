# 项目开发铁律

这份文档是本项目后续开发、调试、部署和交付的最高优先级规范。任何功能开发、修复、重构、部署脚本和文档更新，都必须先遵循这里的要求。

## 1. 项目定位

本项目当前在 Windows 环境开发，最终运行目标是远端 Linux VPS，包括但不限于 Ubuntu、Debian 等发行版。

项目需要长期 7x24 小时自动化运行，默认按无人值守服务设计，而不是一次性脚本或临时调试工具。

后续部署方式以 Docker 为主，所有新增能力都要考虑容器内运行、日志、持久化数据、端口暴露和重启恢复。

## 2. 开发环境兼容

开发时必须同时考虑 Windows 和 Linux：

- 文件路径必须使用 `pathlib.Path` 或跨平台 API，不写死 Windows 反斜杠路径。
- 子进程、信号、进程组、端口释放等逻辑必须检查 Linux 兼容性。
- Shell 命令文档需要分别说明 Windows PowerShell 和 Linux Bash 的差异。
- 不依赖 Windows 专属工具作为运行必需项。
- 文本文件统一使用 UTF-8。
- 换行、文件权限、可执行文件路径都要考虑 Docker/Linux 环境。

如果某个实现只能在 Windows 调试环境工作，不能作为最终方案合入。

## 3. Linux VPS 与 Docker 约束

后续部署以 Docker 为准：

- 应用服务必须能在容器内直接启动。
- 数据库、日志、工具文件必须有清晰的挂载路径。
- 运行配置必须通过环境变量或配置文件控制，不把 VPS 私有路径写进代码。
- 容器重启后，采集、验证、BOT、自动控制等状态要能从数据库或配置中恢复。
- Docker 里不能假设存在交互式终端。
- Docker 里不能依赖桌面 GUI、浏览器窗口或手动点击。

当前默认 Web 管理端口为 `8765`，除非用户明确要求，不能随意改端口。

## 4. 端口规则

所有项目端口必须固定，不能随机漂移。

当前固定端口：

- Web 管理面板：`8765`
- 后续新增服务端口必须先写入本文档，再写入代码或 Docker 配置。

本地调试时，如果固定端口被占用：

1. 先识别占用端口的进程。
2. 如果确认是本项目旧进程或临时 Web 服务，结束该进程。
3. 不为了绕过占用而改成随机端口。
4. 如果占用进程不是本项目相关进程，先向用户说明风险再处理。

## 5. 7x24 自动化运行要求

所有长期任务都必须按服务化思维开发：

- 任务必须可停止、可恢复、可观测。
- 采集、验证、BOT、自动控制不能互相抢占导致死锁。
- 网络失败、源站限流、节点无效、外部命令失败都不能让主服务崩溃。
- 日志必须能帮助定位问题，但不能无限增长。
- 数据写入必须保证中途停止后仍尽量保留已完成结果。
- 数据库操作要考虑并发、锁等待和长期运行后的稳定性。
- 默认配置应保守，避免 CPU、内存、磁盘、端口被耗尽。

新增后台循环、线程、进程时，必须明确：

- 启动条件
- 停止条件
- 异常处理
- 日志输出
- 状态持久化方式
- Docker 重启后的恢复行为

## 6. 数据与持久化

SQLite 数据库是当前核心持久化来源，默认路径为 `data/nodes.db`。

要求：

- 不随意删除或重建用户数据。
- 数据结构变更必须兼容旧库，优先使用迁移逻辑。
- 大功能涉及数据库结构时，必须补测试。
- 导出、订阅、节点处理不得破坏数据库里的原始节点。
- 日志默认写入 `data/logs/`，并应有轮转或大小限制。

## 6.1 总控调度约束

采集、验证、复检、补齐和停止任务必须优先通过 Web 后端总控调度层执行，不能让各模块绕过总控独立抢占运行。

总控必须维护清晰状态：
- `mode`：当前来源，如 `manual`、`auto`、`idle`。
- `intent`：本轮目标，如 `collect`、`validate_pending`、`recheck_valid`、`maintain_pool`。
- `phase`：当前阶段，如 `idle`、`collecting`、`validating`、`rechecking`、`stopping`、`error`。
- `decision`：最近一次调度决策，如 `start_collector`、`start_validator`、`stop_validator_conflict`。
- `last_reason`：面向后台日志和页面展示的可读原因。

运行规则：
- 采集和验证不能同时运行。
- 手动按钮也只是向总控提交请求，不能直接绕过总控启动子进程。
- 自动低水位补齐必须按“先验证待验证库存，库存为空再采集，采集结束再验证”的顺序执行。
- 复检有效节点属于验证类任务，也必须受总控互斥约束。
- BOT 和订阅发放只能读取有效节点池，不参与采集/验证抢占。
- 订阅发放只能通过订阅链接管理生成带 token、次数限制和过期时间的链接；有效节点处理页只做模板、预览和原始节点导出，不直接生成 Base64 订阅。
- 订阅转换只能轻量调用 Sub-Store 兼容服务接口，不下载、不内置完整 Sub-Store 仓库；手动转换必须记录输入协议类型、目标格式、结果状态和错误信息到转换日志。
- 生产/Docker 环境中 Sub-Store 应作为独立 sidecar 服务运行，主系统通过固定内网地址调用，推荐 `http://sub-store:3001`；后台必须提供服务健康检查和可读失败原因。
- 7x24 运行必须接入系统维护策略：新增日志、访问明细、临时记录时，必须提供保留天数或最大条数限制，并纳入后台“系统维护”页和总控自动清理。
- 订阅访问数据只保存必要审计字段和哈希化客户端信息，不保存用户 IP 明文、完整 User-Agent 明文或完整订阅输出内容。
- Windows 停止任务使用进程自身 terminate/kill，Linux/Docker 使用进程组信号；两端都必须可停止、可恢复。

## 6.2 长期运行数据生命周期

长期运行优化必须遵循“核心业务数据长期保留，过程明细短期保留，统计数据长期保留”的原则。

- 核心业务数据包括节点池、有效节点、订阅链接、口令配置、BOT 配置、采集源配置，维护清理不得误删。
- 过程明细包括 BOT 消息日志、订阅访问日志、订阅转换日志、领取记录、验收成功报告，默认只保留 7 天。
- 失败验收报告用于排障，默认保留 30 天。
- 日统计数据默认保留 365 天，用于后台趋势图、运行分析和容量判断。
- 运行日志必须启用大小轮转，默认单文件 10MB，历史备份 5 份，可通过环境变量调整。
- 新增任何会持续增长的数据表、日志文件、报告文件或缓存，都必须同时补充保留天数、最大条数或最大文件数策略，并接入系统维护页面。
- 清理任务必须先刷新统计，再删除过期过程数据；清理后不得影响订阅访问、口令验证、节点导出、采集、验证和 BOT 自动运行。
- 转换缓存应绑定口令版本、节点池 hash、目标格式、导出数量和排序策略；口令版本或节点池变化后旧缓存自动失效。
- 默认运行模式下不保存完整 IP 明文、完整 User-Agent 明文、完整订阅输出和大量逐节点调试日志；需要排障时临时开启详细日志。

## 6.3 长期运行验收和来源质量规则

采集源不能只看“能采到多少节点”，必须同时记录后续验证质量。来源画像至少包含采集成功次数、采集失败次数、采集节点数、验证有效次数、验证无效次数、亚洲有效次数、有效节点平均延迟、最近有效时间和最近无效时间。

节点池必须分层管理：候选节点池负责供给，有效节点库负责质量冗余，精品订阅池负责用户实际订阅输出。有效节点库目标规模为 1000 条，低于该水位时总控必须优先验证候选节点；候选不足时再启动采集补货。

用户订阅不得直接从全部有效节点库随机导出。订阅输出必须经过精品订阅池筛选，默认只导出 20 条质量最高节点。精品池筛选优先低延迟、亚洲节点、近期验证成功、稳定性评分高的节点，并避免同出口 IP、同地区、同协议过度集中。

精品订阅池节点延迟超过阈值或质量不足时，应从精品池移出并回到普通有效节点库继续观察；连续验证失败的节点再从有效库剔除。精品池不足 20 条时，从有效库自动补充；有效库低于水位时继续验证补货。

验证队列必须优先处理更可能真实可用的候选：先按 Xray 当前可验证协议优先，再按来源画像评分排序，再按重复出现次数和最近采集时间排序。低质量来源不能永久屏蔽，仍要保留全球兜底验证机会，避免只依赖少数来源。

验证失败原因必须归类为稳定分类键，例如超时、TLS 错误、协议不支持、DNS 错误、连接拒绝、HTTP 错误、未获取出口 IP、Xray 运行异常、解析错误或其他。页面展示可以使用中文，但数据库统计键必须稳定，便于长期趋势分析。

采集来源连续失败过多时只能进入冷却降权，不能直接禁用或删除来源。冷却期内来源仍可作为全球兜底候选；一旦验证出有效节点，连续失败计数和冷却状态必须自动解除。

真实验收属于高频过程数据，成功报告默认保留 7 天且最多保留有限数量；失败报告用于排障可保留更久。每次启动真实验收前都应先执行轻量报告清理，防止长时间压测在同一天快速堆积文件。

真实验收结果必须写入日统计，只保存总次数、成功数、失败数、格式维度结果和耗时聚合，不新增逐领取明细表。详细报告只作为短期排障文件保留。

日志和访问明细默认只保留 7 天，长期趋势依赖日统计表，不依赖无限增长的明细表。任何新增明细表、报告文件、缓存或日志，都必须先说明是否影响订阅领取、节点验证、BOT 自动运行和系统维护清理。

## 6.4 服务层拆分规则

`web_app.py` 只作为 HTTP 入口、参数校验、权限校验和响应输出层。已经抽出的服务逻辑不得再复制回 `web_app.py`。

当前服务层：
- `services/acceptance_service.py`：真实验收命令构建、验收报告读取、验收报告清理。
- `services/maintenance_service.py`：运行日志和验收报告文件体积统计、维护概览补充。
- `services/converter_service.py`：Sub-Store 目标配置、转换 URL 构建、健康检查、轻量转换调用、转换缓存身份计算。

后续新增服务层时必须遵守：
- 不改变现有 API 路由。
- 不改变固定端口。
- 不绕过总控任务入口。
- 不新增无限增长数据。
- 每次抽取都必须补对应测试。
- 抽取只做结构优化，不借机改业务规则。

## 7. 测试要求

每次完成实质代码变更后，至少运行相关单元测试。

推荐命令：

```bash
python -m unittest discover -v
```

在当前 Windows 开发机上，如果系统 PATH 没有 `python`，可以使用 Codex bundled Python 或项目 `.venv`。

测试失败时必须区分：

- 当前改动引入的新失败
- 项目已有失败
- Windows 文件锁、缺依赖、网络限制等环境问题

不能把未验证的大功能当作完成。

## 8. Git 与 GitHub

完成大功能后必须提交 Git，并推送到 GitHub。

要求：

- 提交前查看变更范围，避免把无关文件、缓存、数据库、日志、虚拟环境提交进去。
- 不提交 `.venv`、`__pycache__`、临时日志、构建残留和用户私密配置。
- 提交信息要清楚描述功能或修复。
- 推送前尽量跑测试。
- 如果当前目录不是 Git 仓库，必须先向用户说明并确认仓库初始化或远程地址。

## 9. 安全与配置

后台账号、BOT Token、订阅 token、服务器地址等敏感信息不能写死到代码或文档示例中。

要求：

- 敏感值存数据库或环境变量。
- API 返回敏感值时必须脱敏。
- 默认账号只用于初始化，后续应支持修改。
- 订阅链接和 BOT 验证链接必须有失效机制。
- 对公网部署时要考虑反向代理、HTTPS、Cookie Secure、访问路径和认证。

## 10. 开发流程

后续每次开发按以下顺序执行：

1. 先理解现有代码和本规范。
2. 明确是否影响 Windows、Linux、Docker、7x24 运行。
3. 修改前确认固定端口和进程状态。
4. 小步实现，优先复用现有模块。
5. 补充或更新测试。
6. 运行测试并记录结果。
7. 更新相关文档。
8. 大功能完成后 Git commit 并推送 GitHub。

## 11. 当前已知项目事实

- 后端入口：`web_app.py`
- 默认后台路径：`/adminhuage`
- 默认 Web 端口：`8765`
- 数据库：`data/nodes.db`
- 前端目录：`web/`
- 运行任务管理：`runtime_tasks.py`
- 服务层目录：`services/`
- 采集器：`node_collector.py`
- 验证器：`node_validator.py`
- 数据库层：`node_database.py`
- BOT：`bot_service.py`

这份文档如需修改，必须是为了让规范更清晰、更严格或更贴近真实部署，不得为了临时方便放宽关键约束。
