# 项目开发铁律

这份文档是本项目后续开发、调试、部署和交付的最高优先级规范。任何功能开发、修复、重构、部署脚本和文档更新，都必须先遵循这里的要求。

## 1. 项目定位

本项目当前在 Windows 环境开发，最终运行目标是远端 Linux VPS，包括 Ubuntu、Debian 等发行版。

项目需要长期 7x24 小时自动化运行，默认按无人值守服务设计，不是一次性脚本或临时调试工具。

后续部署以 Docker 为主，同时兼容宝塔/Nginx 反向代理。

## 2. 固定端口

所有服务端口必须固定，不能随机漂移。

- Web 后台：`8765`
- Sub-Store sidecar：`3001`
- 后台路径：`/adminhuage`

本地调试时，如果固定端口被旧的本项目进程占用，可以结束旧进程；如果不是本项目进程，先说明风险再处理。

## 3. Windows / Linux / Docker 兼容

开发时必须同时考虑 Windows、本地调试、Linux VPS 和 Docker。

- 文件路径使用 `pathlib.Path` 或跨平台 API。
- 子进程、停止任务、信号、进程组必须考虑 Windows 和 Linux 差异。
- 不依赖 GUI、桌面浏览器或交互式终端作为生产必需条件。
- 文本文件统一 UTF-8。
- Xray 路径必须兼容 `tools/xray/xray` 和 `tools/xray/xray.exe`。
- Docker 环境中所有数据和日志必须挂载到明确目录。

## 4. 总控调度

采集、验证、复检、补齐、BOT、验收和维护都必须优先通过 Web 后端总控调度层执行。

总控必须维护清晰状态：

- `mode`：任务来源，例如 `manual`、`auto`、`idle`
- `intent`：本轮目标，例如 `collect`、`validate_pending`、`recheck_valid`
- `phase`：当前阶段，例如 `idle`、`collecting`、`validating`、`rechecking`
- `decision`：最近一次调度决策
- `last_reason`：后台页面和日志可读原因

运行规则：

- 采集和验证不能同时运行。
- 手动按钮也只是向总控提交请求，不能绕过总控抢占任务。
- 自动补货顺序是：先验证待验证库存，库存不足再采集，采集结束再验证。
- BOT 和订阅分发只读取有效节点池，不参与采集/验证抢占。
- 真实验收只能调用现有后台 API、BOT 模拟、公开订阅入口和 Sub-Store 转换接口。

## 5. 节点池逻辑

节点流转顺序：

```text
采集源 -> 原始候选节点库 -> Xray 验证 -> 有效节点库 -> 精品订阅池 -> 用户订阅
```

有效节点库目标规模至少 `1000` 条。用户订阅默认只导出质量最高的 `20` 条。

订阅输出必须优先：

- 低延迟
- 亚洲节点加权
- 最近验证成功
- 稳定性评分高
- 避免同出口 IP、同地区、同协议过度集中

有效节点低于水位时，总控必须优先验证已有候选；候选不足再采集。

## 6. 采集源画像

采集源不能只看“采到多少节点”，还必须记录后续验证质量。

来源画像至少包含：

- 采集成功次数
- 采集失败次数
- 采集节点数量
- 验证有效次数
- 验证无效次数
- 亚洲有效次数
- 平均有效延迟
- 最近有效时间
- 最近无效时间
- 连续无效次数
- 冷却时间
- 最近失败分类

低质量来源只能冷却降权，不能直接删除或永久禁用。冷却来源仍可作为全球兜底候选。

验证队列必须优先处理更可能可用的候选：

1. Xray 当前可验证协议优先。
2. 来源画像评分高优先。
3. 重复出现次数高优先。
4. 最近采集时间新优先。

## 7. 验证策略

Xray 验证必须记录：

- 连通性
- 延迟
- 出口 IP
- 国家/地区
- 失败原因分类

失败原因必须使用稳定分类键，例如：

- `timeout`
- `tls_error`
- `unsupported_protocol`
- `dns_error`
- `connection_refused`
- `http_error`
- `no_proxy_ip`
- `xray_runtime`
- `parse_error`
- `other`

验证不能无限硬跑低质量批次。真实验收或自动验证如果连续多批没有新增有效节点，必须止损、换源或进入下一轮策略。

候选节点验证失败可以从待验证库剔除，但要保留统计和来源画像，不得让无效明细无限增长。

## 8. 订阅与口令

订阅链接必须绑定：

- 随机 token
- 启用/禁用状态
- 访问次数限制
- 过期时间限制
- 当前领取口令版本

口令版本变更后，旧版本订阅链接必须失效，不能继续指向真实节点。

口令有效期和每日领取次数是领取入口限制；口令过期不等于已经发出的有效订阅自动过期，订阅是否失效由订阅链接自己的限制和口令版本控制。

订阅转换只能轻量调用 Sub-Store 兼容服务接口，不下载、不内置完整 Sub-Store 仓库。

生产/Docker 环境中 Sub-Store 应作为独立 sidecar 服务运行，推荐地址：

```text
http://sub-store:3001
```

宝塔 Python 项目方式部署时，Sub-Store 可以单独监听：

```text
http://127.0.0.1:3001
```

## 9. 数据生命周期

核心业务数据长期保留：

- 节点池
- 有效节点
- 订阅链接
- 口令配置
- BOT 配置
- 采集源配置
- 来源画像统计

过程明细短期保留：

- BOT 消息日志
- 订阅访问日志
- 订阅转换日志
- 领取记录
- 验收成功报告

默认过程明细只保留 7 天。失败验收报告可保留 30 天。日统计数据默认保留 365 天。

新增任何持续增长的数据表、日志文件、报告文件或缓存，都必须同时提供：

- 保留天数
- 最大条数或最大文件数
- 系统维护清理入口
- 清理后不影响订阅领取、节点验证、BOT 自动运行和系统维护

## 10. Git 与发布

完成大功能后必须提交 Git 并推送 GitHub。

提交前必须排除：

- `.venv/`
- `__pycache__/`
- `data/`
- `build/`
- 运行日志
- SQLite 数据库
- Xray 二进制和 geo 数据
- Sub-Store bundle 和运行数据
- 本地私密配置 `.env`

版本发布使用标签，例如：

```text
v1.1.0
```

## 11. 测试要求

每次实质代码变更后，至少运行相关单元测试。发布前尽量运行：

```bash
python -m unittest discover -v
```

测试失败必须区分：

- 当前改动引入的新失败
- 项目已有失败
- Windows 文件锁、网络限制、依赖缺失等环境问题

不能把未验证的大功能当作完成。

## 12. 当前项目事实

- 后端入口：`web_app.py`
- 默认后台路径：`/adminhuage`
- 默认 Web 端口：`8765`
- 默认数据库：`data/nodes.db`
- 前端目录：`web/`
- 服务层目录：`services/`
- 采集器：`node_collector.py`
- 验证器：`node_validator.py`
- 数据库层：`node_database.py`
- BOT：`bot_service.py`
- 开发规则文档：`00_DEVELOPMENT_RULES.md`
- 宝塔部署文档：`DEPLOY_BAOTA.md`
