# Development Log

## 2026-05-24

### 开发工具或 AI

- Codex

### 本次任务

- 继续补齐用户强调的第二阶段能力，重点完成 Xray-core 真实代理检测，不重复改动已经完成的 UI、中文化、采集、测试、导出、领取页和反馈统计。

### 实现思路

- 保留原有 TCP 基础初筛，把 Xray-core 作为第二级真实检测，只检测 `test_passed` 候选节点。
- 按节点协议生成临时 Xray 配置，每次只监听 `127.0.0.1` 随机本地端口，再通过本地 HTTP 代理访问 `XRAY_TEST_URL`。
- 使用低并发、单节点超时、进程关闭和临时目录清理保护轻量 VPS。
- 将发布前复测设计为可选检查接口，只给通过率和风险提示，不强制阻止发布。

### 已完成

- Xray-core 真检测支持 VLESS、VMess、Trojan、Shadowsocks 的第一阶段转换。
- 支持 TLS、Reality、WebSocket、gRPC 常见参数的 Xray `streamSettings` 生成。
- 检测成功后记录真实代理延迟、真实状态、质量档位、测试方式、成功次数和入包资格。
- 检测失败后记录脱敏失败原因，并避免把完整节点配置写入日志或公开接口。
- 未配置 Xray 或协议暂不支持时只记录跳过状态，不破坏原有 TCP 候选节点。
- 新增发布前批次检查接口 `POST /api/export-batches/:id/preflight`。
- `XRAY_TEST_URL` 默认改为 HTTP 204 地址，同时保留 HTTPS CONNECT 支持。

### 遇到的问题

- 当前本地环境没有 `npm`，系统 `node.exe` 被拒绝执行，因此不能在本地运行 `npm run typecheck` 或 `npm run build`。
- 真实 Xray 检测必须在 VPS 上安装/配置 xray-core 后才能端到端验证。

### 后续建议

- 在 VPS 上配置 `XRAY_REAL_TEST_ENABLED=true`、`XRAY_CORE_PATH=/usr/local/bin/xray` 后，先用少量节点测试 Xray 检测。
- Hysteria2、TUIC 和 sing-box 专属格式后续用 sing-box 检测扩展。
- 后续把质量档位阈值做成系统设置，而不是固定默认规则。

## 2026-05-24

### 开发工具或 AI

- Codex

### 本次任务

- 检查已完成内容，不重复推翻；补齐质量档位、标准节点包、自动化接口和 Xray-core 真实检测安全骨架。

### 实现思路

- 不改现有采集、基础测试、导出、公开领取页和后台登录逻辑。
- 在 SQLite 结构上追加字段，使用轻量迁移兼容已有 VPS 数据库。
- Xray-core 默认关闭，避免轻量 VPS 因真实检测误触高负载。
- 自动化接口只读，并使用 `AUTOMATION_API_TOKEN` 控制访问。

### 已完成

- 增加节点真实检测字段和质量档位字段。
- 增加节点包权限字段和质量档位字段。
- 生成节点包时增加 `clash.yaml`、`sing-box.json`、`README.txt`。
- 导出页面支持延迟区间、协议、档位、是否真实检测、是否口令、公开领取和自动化权限。
- 新增 `POST /api/xray-test-runs`。
- 新增 `/api/automation/*` 只读接口和后台自动化接口页面。
- 系统设置页面补充 Xray、质量档位、自动化接口说明。

### 未解决问题

- Xray-core 当前是安全接入骨架，尚未完成每种协议到临时 Xray 配置的完整转换。
- `clash.yaml` 和 `sing-box.json` 当前为安全模板，主导入文件仍是 `nodes.txt`。
- 本地没有 `npm`，未执行完整 TypeScript 构建。

### 后续建议

- 在 VPS 安装/确认 xray-core 后，逐步实现 VLESS、VMess、Trojan、Shadowsocks 到临时 Xray 配置的转换。
- 真实检测必须继续保持 127.0.0.1、本地随机端口、低并发、超时清理。
- sing-box 专属协议 Hysteria2、TUIC 后续单独扩展，不在当前阶段强行做完。

## 2026-05-24

### 开发工具或 AI

- Codex

### 本次任务

- 在不重复改动既有发布/领取架构的前提下，补齐反馈处理闭环。

### 实现思路

- 后台继续通过登录鉴权接口处理反馈，不对公开领取页暴露后台数据。
- 前端反馈页只增加最小操作按钮，避免重做页面结构。
- 处理状态仍使用后端英文枚举保存，前端统一显示中文。

### 已完成

- 新增 `PATCH /api/feedback/:id`，支持更新反馈处理状态和可选处理备注。
- 反馈数据页增加“已查看 / 已解决 / 需重做包”操作。
- 调整反馈表格列宽，避免新增操作列后挤压主要内容。

### 未解决问题

- 当前本地环境缺少 `npm`，无法执行本地完整构建。
- 需要在 VPS Docker 环境验证反馈处理按钮、公开反馈提交、渠道统计联动。

### 后续建议

- 如需更完整的反馈后台，可再补充“无效反馈”和“处理备注编辑”弹窗。
- 保持现有采集、测试、导出和领取逻辑稳定，不做重复重构。

## 2026-05-23

### 开发工具或 AI

- Codex

### 本次任务

- 开发 v0.1.0 项目基础版。

### 实现思路

- 使用单体部署形态降低 VPS 部署复杂度：Fastify 后端提供 API，同时托管 Vite 构建后的前端静态文件。
- 使用 SQLite 作为默认数据库，Docker volume 持久化 `/data`。
- 先建立后续版本需要的数据表，避免后续采集、测试、导出功能反复调整基础结构。
- 登录系统从第一阶段开始落地，避免后台裸奔。

### 遇到的问题

- 当前 Windows 环境中直接调用 `node.exe` 被拒绝访问。
- 当前环境没有 `npm`、`docker`、`gh` 命令。
- 当前 Git 仓库没有 remote。

### 解决方式

- 继续完成源码与部署文件。
- 使用 Codex bundled Node 路径执行无依赖敏感检查脚本。
- 用 PowerShell 补充扫描敏感文件名和 Token 模式。

### 未解决问题

- 尚未确认 GitHub 私有远程仓库地址。
- 尚未完成依赖安装和构建验证。
- 尚未完成 GitHub push。

### 后续建议

- v0.2.0 前先保证 GitHub 私有仓库可推送。
- v0.2.0 实现采集任务时必须先实现限速、缓存、失败降频，再增加来源发现。

## 2026-05-23 v0.2.0

### 开发工具或 AI

- Codex

### 本次任务

- 开发节点采集与解析版。

### 实现思路

- 采集入口先以 GitHub 公共仓库发现和管理员配置的公开 URL 种子为主。
- 所有采集都走来源缓存、冷却时间、并发限制、请求超时和大小限制。
- 第一版只提取节点链接和协议类型，不做深度字段解析。
- 节点使用内容 SHA-256 做完全去重。

### 遇到的问题

- v0.1.0 中部分中文 UI 文案在当前 Windows PowerShell 环境显示为乱码，前端 JSX 存在语法风险。
- 当前环境仍没有 `npm`、`docker`、`gh`。

### 解决方式

- 重写 `apps/api/src/routes.ts`，移除损坏文案。
- 重写 `apps/web/src/main.tsx`，先修复损坏 JSX，再恢复中文 UI 文案。
- 配置 GitHub remote：`https://github.com/webhuage-debug/jiandiancaiji.git`。

### 未解决问题

- 尚未在 Docker/Linux 环境真实运行采集。
- 尚未 push，需在本阶段提交后执行。

### 后续建议

- 在 VPS 或 Docker 环境执行 `npm install`、`npm run build` 和一次采集。
- v0.3.0 开始实现基础连通性测试与候选节点池。

## 2026-05-23 v0.3.0

### 开发工具或 AI

- Codex

### 本次任务

- 开发节点测试与候选池版。

### 实现思路

- 不启动代理服务，不开放端口，只从节点链接解析 host/port 后做出站 TCP 连接初筛。
- 测试通过写入 `test_passed` 和 `latency_ms`。
- 测试失败写入 `test_failed` 和 `failure_reason`，不进入候选池。
- 仪表盘展示测试批次、节点池预览和延迟分布。

### 遇到的问题

- 当前环境无法执行 npm/Docker 构建，也无法真实访问 GitHub 进行 push 或采集验证。

### 解决方式

- 使用源码级检查和敏感文件检查确认本阶段改动。
- 将网络/DNS 阻塞写入交接日志。

### 未解决问题

- 未在 Linux VPS 上执行真实 TCP 测试。
- 未执行 TypeScript 类型检查。

### 后续建议

- 在 VPS 环境完成 `npm install`、`npm run build`、采集和测试联调。
- v0.4.0 实现导出批次、纯节点文件和加密 zip 包。

## 2026-05-23 v0.4.0

### 开发工具或 AI

- Codex

### 本次任务

- 开发节点包导出版。

### 实现思路

- 从 `test_passed` 节点中按后台初筛延迟排序选择导出节点。
- 默认排除已导出节点，支持自定义导出数量和延迟范围。
- 生成纯节点文本文件，避免影响客户端识别。
- 生成说明文件和免责声明，再通过系统 `zip -P` 打包加密。
- 口令只保存 bcrypt 哈希，不保存明文。

### 遇到的问题

- 当前本地环境没有 npm、docker，也未确认系统 zip 命令可用。
- 当前环境仍无法解析 `github.com`，无法 push。

### 解决方式

- Docker runtime 镜像安装 `zip`。
- 将 zip 依赖和验证缺口写入交接日志。

### 未解决问题

- 未在 Linux/Docker 环境实际生成 zip 包。
- 未执行 TypeScript 类型检查。

### 后续建议

- 在 VPS 上完成一次真实导出验证。
- v0.5.0 实现公开领取页、口令验证、下载统计和反馈入口。

## 2026-05-23 v0.5.0

### 开发工具或 AI

- Codex

### 本次任务

- 开发公开领取页版。

### 实现思路

- 使用导出批次的 `public_slug` 作为公开领取页路径。
- 公开 API 只返回批次说明和数量，不展示完整节点。
- 口令验证成功后设置短时 HttpOnly 解锁 Cookie。
- 下载 API 必须检查解锁 Cookie。
- 访问、口令、下载、反馈全部写入统计表和事件表。

### 遇到的问题

- 当前环境前端文件曾出现编码损坏，已重写 `apps/web/src/main.tsx`。
- 当前环境无法执行 npm/Docker 构建。
- 当前环境无法解析 `github.com`。

### 解决方式

- 重写前端入口，保持中文 UI。
- 遵循用户要求：不主动 push GitHub，只本地提交，等待明确推送指令。

### 未解决问题

- 未在 Linux/Docker 环境验证公开领取页下载流程。
- 未执行 TypeScript 类型检查。

### 后续建议

- 在 VPS 上完成公开领取页端到端验证。
- v0.6.0 实现完整公开视频模式、日志脱敏、下载限速和安全加固。

## 2026-05-23 v0.6.0

### 开发工具或 AI

- Codex

### 本次任务

- 开发安全与公开视频模式版。

### 实现思路

- 公开视频模式保存到后端设置，避免刷新后丢失。
- 后端在公开视频模式下对节点池、来源缓存、日志接口脱敏。
- 新增统一脱敏工具，隐藏节点链接、Token、UUID 和完整 IPv4。
- 公开下载接口加入分钟级限速。

### 遇到的问题

- 当前环境无法执行 npm/Docker 构建。
- 当前环境无法解析 GitHub，但用户要求不要主动 push。

### 解决方式

- 仅做本地开发、本地检查和本地提交。
- 不执行 `git push`，等待用户明确指令。

### 未解决问题

- 未在 Linux/Docker 环境验证下载限速。
- 未执行 TypeScript 类型检查。

### 后续建议

- v1.0.0 做端到端验收、部署文档校正和最终安全检查。

## 2026-05-23 v1.0.0

### 开发工具或 AI

- Codex

### 本次任务

- 第一版正式可用版收口。

### 实现思路

- 不再扩展大功能，补齐正式版版本号、统计接口、反馈接口和发布检查清单。
- 保持运行数据和软件源码分离。
- 将仍需 VPS/Docker 环境验证的项目明确写入 release checklist。

### 遇到的问题

- 当前环境仍无法执行 npm/Docker 构建。
- 当前环境仍无法访问 GitHub。

### 解决方式

- 只做本地提交，不 push。
- 在 `docs/RELEASE_CHECKLIST.md` 中列出 VPS/Docker 必验项。

### 未解决问题

- 尚未在 Linux VPS 上完成端到端验收。
- 尚未 push 到 GitHub，等待用户明确指令。

### 后续建议

- 在 VPS 上执行 release checklist。
- 验证无误后再按用户指令统一 push GitHub。

### 追加备注

- 用户要求补充 VPS 一键部署能力。
- 已新增 `scripts/deploy-vps.sh`。
- 已在 `README.md` 和 `docs/DEPLOY.md` 写入一键部署命令。
- 私有仓库部署时推荐使用只读 GitHub Token，通过 `GITHUB_TOKEN` 环境变量传入，不写入 `.env`。
# 2026-05-23 修复后台交互与 UI 最小重构

## 开发工具或 AI
- Codex

## 本次任务
- 修复后台按钮点击无明显反馈、菜单不切换、领取页入口不清晰的问题。
- 按 `docs/ui-reference/` 参考风格做最小代价 UI 调整。
- 保持公开领取页极简，避免展示后台内部信息。

## 实现思路
- 保留现有 React/Fastify/SQLite 架构，不重写业务模块。
- 前端增加真实菜单状态，让左侧菜单进入独立页面。
- 前端统一 API 请求携带 `credentials: "same-origin"`。
- 节点包列表提供“打开领取页”链接，方便直接进入公开领取页。
- 公开领取页删除节点数量、内部批次编号等不必要信息。
- CSS 参考图采用白色侧栏、浅色背景、绿色主色、白色卡片、清晰表格。

## 遇到的问题
- 本地原分支落后远程 3 个提交，远程新增了 `docs/ui-reference/` 图片和 README。
- 本地环境没有可用 `npm` 命令，无法直接执行前端构建。
- 当前没有 VPS SSH 会话和后台真实密码，无法由 Codex 直接完成 VPS 端 curl 登录和 Docker 日志验证。

## 解决方式
- 已合并 `origin/codex/v1.0.0-release`，保留用户上传的 UI 参考图。
- 使用本地图片查看参考 UI，按最小范围调整前端页面与样式。
- 使用敏感文件检查和 UTF-8 扫描代替本地构建检查。

## 未解决问题
- VPS 端真实 Docker、curl 登录、浏览器点击验证仍需用户提供 SSH 结果或授权远程登录后继续。
- Xray-core 真实代理检测尚未实现。

## 后续建议
- 在 VPS 执行 `git pull --ff-only origin codex/v1.0.0-release && docker compose up -d --build` 后验证采集、测试、节点包和领取页。
- 后续单独开发 Xray-core 二级真实代理检测，不要混入本次 UI 修复。

# 2026-05-24 修复节点包 Windows 解压文件名乱码

## 开发工具或 AI
- Codex

## 本次任务
- 修复节点包在 Windows 解压后中文文件名乱码的问题。
- 修复导出说明模板中文内容被编码污染的问题。

## 实现思路
- 不依赖 zip 工具对中文文件名编码的兼容性。
- 将压缩包内部文件名改为 ASCII，保证 Windows、macOS、Linux、安卓解压都稳定。
- 保留说明文件中文内容，使用 UTF-8 写入。

## 修改内容
- `apps/api/src/exporter/exportService.ts`：重写导出服务文件名和说明模板。
- `docs/HANDOFF.md`：追加本次修复记录。

## 后续建议
- VPS 更新后重新生成节点包；旧节点包不会自动变化。
- 检查 `nodes.txt` 是否仍为纯节点内容。

# 2026-05-24 重构节点包发布机制与公开领取端

## 开发工具或 AI
- Codex

## 本次任务
- 在不破坏采集、测试、导出和节点包生成的基础上，分离后台管理端与粉丝公开领取端。
- 将“生成节点包”和“公开发布”拆成两个动作。

## 实现思路
- 批次生成时一律进入 `draft` 状态，不自动公开。
- 管理员手动发布批次；发布新批次时旧的 `published` 批次自动变为 `replaced`。
- 公开领取页改为 `/r/:slug`，公开 API 改为 `/api/public/claim/:slug`。
- 公开 API 只返回领取必要信息，不返回节点数量、后台统计、文件路径或内部批次详情。
- 新增渠道统计接口，按 `from` 参数聚合访问、口令输入和下载。

## 遇到的问题
- 原有生成批次会立即把节点标记为 `exported`，草稿也会影响后续导出。

## 解决方式
- 新增 `export_batch_nodes` 关联表。
- 草稿只记录关联，不修改节点状态。
- 发布批次时再将该批次关联节点标记为已导出。

## 未解决问题
- 尚未在 VPS Docker 环境完整验证。
- Cloudflare Tunnel / Caddy / HTTPS 域名隔离仍为后续部署规划，当前不改 80/443。

# 2026-05-24 完善中文化、统计反馈和录屏展示语义

## 开发工具或 AI
- Codex

## 本次任务
- 全局统一中文状态展示，避免直接显示后端英文状态。
- 将统计页重心调整为渠道分发效果。
- 将反馈页调整为节点使用反馈分析。
- 在公开领取页加入轻量反馈入口。
- 补强系统设置和运行日志页面的信息结构。

## 实现思路
- 不重复推翻已有 UI 和发布分离逻辑。
- 前端新增统一映射函数：状态、失败原因、问题类型、处理状态、渠道名称、日志级别。
- 后端反馈表补充问题类型、来源渠道和处理状态字段。
- 公开反馈 API 只接受枚举字段和短备注，保护隐私并减少刷屏风险。

## 未解决问题
- 反馈处理状态的后台按钮仅完成数据结构和展示，后续可补 PATCH 接口做状态流转。
- VPS Docker 构建和端到端验证仍需在服务器执行。
